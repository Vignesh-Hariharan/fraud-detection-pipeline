name: 'fraud_detection'
version: '1.0.0'
config-version: 2

profile: 'fraud_detection'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"
  - "logs"

models:
  fraud_detection:
    staging:
      +materialized: view
      +schema: raw
    intermediate:
      +materialized: table
      +schema: marts
    marts:
      +materialized: table
      +schema: marts

